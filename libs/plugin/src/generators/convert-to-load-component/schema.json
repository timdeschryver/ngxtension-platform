{
	"$schema": "http://json-schema.org/schema",
	"$id": "",
	"title": "",
	"type": "object",
	"properties": {
		"project": {
			"type": "string",
			"aliases": ["name", "projectName"],
			"description": "Project for which to convert to loadComponent."
		},
		"path": {
			"type": "string",
			"description": "Path to the routes file you want to convert to loadComponent."
		}
	}
}
